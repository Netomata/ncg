Fix bindings for erb template -- should only see net, target_key, and target
Change auto-key prefix (to "@"? "%"?)
Set up ARGV processing in ncg
Add file/line info to error messages
Fix switch templates to break out VRRP & shutdown/no-shutdown config fragments
